# Application runtime deps (ingestion + serving + dashboard + analysis).
# Used by Dockerfile (the "app" image). Spark runs in its own image
# (Dockerfile.spark) which bundles PySpark + the s3a/JDBC jars.

# --- Ingestion ---
httpx==0.27.2
tenacity==8.5.0          # retry / exponential backoff for respectful scraping
PyYAML==6.0.2            # skills gazetteer
pymongo==4.8.0           # raw NoSQL store
boto3==1.35.36           # MinIO (S3-compatible) client

# --- Serving ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
psycopg2-binary==2.9.9   # PostgreSQL driver for API + loaders
pydantic==2.9.2

# --- Dashboard + analysis ---
streamlit==1.38.0
pandas==2.2.2
plotly==5.24.1
requests==2.32.3         # dashboard -> API calls
matplotlib==3.9.2        # demand_analysis notebook

# Optional, for running Spark jobs directly on the host (not needed with Docker):
# pyspark==3.5.1
